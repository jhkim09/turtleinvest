# 프로덕션 환경변수 (AWS 배포용)
# 실제 배포시 이 값들을 AWS 환경에 맞게 수정하세요

# 데이터베이스
MONGODB_URI=mongodb://localhost:27017/eap-service-prod
# AWS DocumentDB 사용시: mongodb://username:password@your-cluster.cluster-xxxxxxxxx.region.docdb.amazonaws.com:27017/eap-service?ssl=true&replicaSet=rs0&readPreference=secondaryPreferred&retryWrites=false

# JWT 보안
JWT_SECRET=your_super_secure_jwt_secret_for_production_change_this_value
JWT_EXPIRES_IN=24h

# 서버 설정
NODE_ENV=production
PORT=3000

# CORS 설정 (프론트엔드 도메인으로 변경)
FRONTEND_URL=https://your-frontend-domain.com
ALLOWED_ORIGINS=https://your-frontend-domain.com,https://your-admin-domain.com

# AWS 설정 (필요시)
# AWS_REGION=ap-northeast-2
# AWS_ACCESS_KEY_ID=your_access_key
# AWS_SECRET_ACCESS_KEY=your_secret_key

# 로그 설정
LOG_LEVEL=info

# 보안 설정
BCRYPT_ROUNDS=12
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# 파일 업로드 (필요시 S3 설정)
# S3_BUCKET_NAME=your-s3-bucket-name
# S3_REGION=ap-northeast-2

# 이메일 서비스 (필요시 SES 설정)
# EMAIL_SERVICE=SES
# EMAIL_REGION=ap-northeast-2
# EMAIL_FROM=noreply@your-domain.com